# v1.2 Backend portability

## Goal

Lift the current hard-wired Claude Code and Codex CLI branching in agent_os/cli_backends.py into a BackendAdapter registry that new adapters can plug into, so switching provider does not require graph or dispatcher changes.

## Motivation

- v1.1 shipped two hard-coded CLI backends for Claude Code and Codex.
- Antigravity is a candidate backend, but it must be gated on stable noninteractive output and enforceable permission modes.
- Operators need a health check that reports installed binaries, authentication state, and configured roles before a workflow starts.
- Operators need named profiles instead of ad-hoc environment exports for common router, architect, executor, and sandbox combinations.
- Backend selection must be visible and durable enough to make resume behavior predictable.

## In scope

- Introduce a BackendAdapter registry so architect and executor roles can target additional CLI or API providers without touching graph code.
- Add an agent-os doctor command that reports installed binaries, authentication status, and configured roles in a machine-readable format.
- Support named configuration profiles that select router, architect, executor, and sandbox defaults in one switch.
- Add ROUTER_MODE=direct-escalation to bypass Tier-2 structured routing when the operator has already chosen an architect-first workflow.
- Persist the resolved backend selection inside the SQLite checkpoint so a resumed thread continues on the same provider it started with.
- Add a candidate Antigravity CLI adapter behind the registry once its noninteractive output and permission contract can be enforced.

## Out of scope

- Skill packages remain v1.3 scope.
- The Hermes bridge remains v1.3 scope.
- Vault memory remains v1.4 scope.
- Context profiles remain v1.5 scope.
- REPL and TUI interfaces remain v1.6 scope.
- Ambient interfaces remain v2.0 scope.
- OS-level isolation remains v2.1 deployment territory.

## Contracts

### BackendAdapter interface

Define a small adapter boundary for every provider that can serve the architect or executor graph role. The existing CLI runner logic in agent_os/cli_backends.py becomes the reference adapter implementation for Claude Code and Codex.

```python
BackendRole = Literal["architect", "executor"]
BackendArtifact = ArchitectBrief | ExecutorReport
BackendInvoker = Callable[[AgentState], BackendArtifact]


class BackendAdapter(Protocol):
    name: str
    binary_name: str
    supported_roles: frozenset[BackendRole]

    def build_invoker(self, role: BackendRole) -> BackendInvoker: ...
    def authentication_status(self) -> AuthStatus: ...
```

Build invokers per role. Each invoker consumes AgentState and returns the typed artifact for that role: ArchitectBrief for architect or ExecutorReport for executor. Implementations may use overloads on build_invoker() to preserve the exact return type for each role. The binary_name field lets agent-os doctor perform a PATH check for every adapter.

The router is not an adapter role. It remains a LiteLLM structured-output model rather than a CLI delegator. Register architect and executor adapters by backend name and role, matching the existing LLM_ARCHITECT=cli/claude-code and LLM_EXECUTOR=cli/codex convention. Keep graph nodes and dispatcher code dependent on the registry contract, not provider-specific branches.

The argument denylist in agent_os/cli_backends.py is a global security policy owned by the shared runner. Adapters cannot replace or weaken it; a provider-specific denylist may only union-extend the global set. Permission modes and other fixed arguments remain internal to each adapter, so the public contract does not expose role_config(). The contract also has no cleanup() method: every adapter must self-clean temporary and schema files with context managers inside each invocation.

### agent-os doctor

Add a command that reports whether the local installation can run the configured backend graph. Output must support both machine-readable JSON and a human-readable summary table.

```bash
agent-os doctor --json
```

Include these fields in the doctor report:

- Registered adapters and their supported architect or executor roles.
- PATH resolution for each required binary.
- authentication_status() result for each adapter, with secrets and tokens redacted.
- Active profile name, if any.
- Resolved router, architect, executor, and sandbox.
- checkpoints.db reachability.
- Warnings for missing or misconfigured items.

Return exit code 0 when the configuration is healthy. Return exit code 1 when any required check fails.

### Config profiles

Use named profiles to switch router, architect, executor, and sandbox in one flag. Read profiles from $XDG_CONFIG_HOME/agent-os/profiles.toml, with fallback to ~/.config/agent-os/profiles.toml.

```toml
[profile.local]
router = "ollama/llama3.1"
architect = "cli/claude-code"
executor = "cli/codex"
sandbox = "./sandbox"
description = "Local router with subscription CLI agents"
```

Profile entries use [profile.<name>] with router, architect, executor, sandbox, and description keys. Resolve profile selection in this order:

- --profile CLI flag.
- AGENT_OS_PROFILE environment variable.
- Default profile marked in the file.
- Current environment-variable behavior.

Keep current environment-variable behavior backward compatible. Never store secrets in profiles.

#### Profile inheritance

Support one-level profile inheritance in v1.2 with an extends key inside [profile.<name>]. The child inherits every key from the parent; keys defined on the child override the parent's value. Keep inheritance single-parent and one level deep. Do not implement diamond resolution.

```toml
[profile.cli-base]
router = "ollama/llama3.1"
architect = "cli/claude-code"
executor = "cli/codex"
sandbox = "./sandbox"

[profile.cli-write]
extends = "cli-base"
sandbox = "./worktree-sandbox"
```

- Circular extends chains fail loading with a clear error.

### ROUTER_MODE=direct-escalation

Introduce a new ROUTER_MODE environment variable with cascade as the default. In direct-escalation mode, do not initialize or call the Tier-2 structured router at all; every task that misses Tier 1 goes straight to architect.

Cascade mode preserves the current router behavior unchanged.

Use this mode for operators who have already decided on an architect-first workflow, typical for a CLI Architect plus CLI Executor loop. Avoid spending a model call on structured routing when the operator wants escalation anyway.

Keep cascade as the default router mode. Ensure direct-escalation still respects human_gate; architect plans must still pause for approval before executor work starts.

### Checkpoint backend persistence

Write the resolved backend selection into AgentState at graph start and preserve it through the SQLite checkpointer. On --resume, read the persisted selection and reject a conflicting environment override with a clear error instead of silently switching providers mid-thread.

By default, fail on a conflicting environment override at resume. Compare effective values rather than profile labels: changing only profile_name is not a conflict when router, architect, executor, and sandbox_root resolve identically. Name the persisted binding and every conflicting effective override in the error. Recommend either editing the environment or profile to match, or passing --force-rebind.

Accept --force-rebind at the CLI. Before rebinding, fully validate that each adapter exists, supports its assigned role, and that the sandbox path is valid. Apply the update atomically so failed validation leaves the old checkpoint binding untouched.

On a valid forced rebind, emit a warning that lists every old → new effective-value change and names the current resume node, then update the persisted binding and continue. Always emit this warning; never silence it. If the workflow is resuming at executor after a failure, the warning must also state that partial edits may already exist in the sandbox and instruct the operator to inspect them before continuing.

Use this state key:

```python
state["backend_binding"]: BackendBinding
```

Define BackendBinding as a Pydantic model with router, architect, executor, profile_name, and sandbox_root fields. Normalize sandbox_root to an absolute path before storing or comparing bindings. Store provider names and role bindings, not credentials.

### Antigravity adapter (candidate)

Keep the Antigravity adapter behind the registry and opt-in only. Gate support on both acceptance criteria:

- A documented noninteractive invocation form with parseable output.
- A permission mode the runner can pin equivalent to Claude plan and acceptEdits or Codex read-only and workspace-write.

Until both criteria are documented by the vendor, ship the adapter as a stub that returns a clear "not yet supported" error. Match the roadmap position that Antigravity is a candidate, not a supported backend.

List the Antigravity adapter in agent-os doctor with status "not-yet-supported". Include a short actionable message that names the two acceptance criteria: noninteractive form and enforceable permission mode. Point operators to docs/roadmap.md v1.2 or the adapter module docstring for updates.

When invoked directly, the stub returns the same "not yet supported" error and lists the acceptance criteria.

## Sub-milestones

- R1.2a: BackendAdapter interface, registry, and migration of the existing CLI delegators. Keep this as one PR with two reviewable micro-tasks/commits:
  1. Define the contract and normalized registry, including collision detection and role validation.
  2. Add Claude Code and Codex adapters, then migrate the existing architect and executor factories onto the registry.
  Files touched: agent_os/agents/cli_architect.py, agent_os/agents/cli_executor.py, a new registry/adapter module, tests/test_cli_architect.py, tests/test_cli_executor.py, new registry tests, and node parity tests if the resolution path changes.
  Definition of done: existing Claude Code and Codex paths run through the registry with no behavior change; no hard-coded ("claude-code", "codex") tuple remains in the factories; Claude plan/acceptEdits and Codex read-only/workspace-write permission modes remain exact; the Codex-only strict-schema path is unchanged; architect retry and executor no-retry semantics are unchanged; and tests continue covering temporary-file cleanup, auth-error redaction, and global bypass-argument blocking.

- R1.2b: agent-os doctor command.
  Files touched: agent_os/cli/app.py, CLI command wiring, and doctor support module.
  Test additions: JSON output tests, human summary tests, exit-code tests, and redaction tests.
  Definition of done: agent-os doctor reports adapters, PATH resolution, authentication status, active profile, resolved roles, sandbox, and checkpoints.db reachability.

- R1.2c: Config profiles.
  Files touched: CLI configuration loader and profile parser module.
  Test additions: TOML parsing tests, selection precedence tests, and backward-compatible environment-variable tests.
  Definition of done: --profile selects router, architect, executor, and sandbox while profiles never store secrets.

- R1.2d: ROUTER_MODE=direct-escalation.
  Files touched: agent_os/routing.py and agent_os/nodes/tool_dispatcher.py.
  Test additions: router-mode tests covering Tier-1 hits, Tier-1 misses, and human_gate preservation.
  Definition of done: direct-escalation skips Tier-2 structured routing for Tier-1 misses and leaves cascade as the default.

- R1.2e: Checkpoint backend persistence and resume-time conflict detection.
  Files touched: agent_os/state.py, graph startup wiring, resume CLI wiring, and checkpoint serialization tests.
  Test additions: AgentState backend_binding tests, SQLite resume tests, and conflicting override tests.
  Definition of done: resumed threads keep their original backend binding or fail with a clear conflict error.

- R1.2f: Antigravity stub adapter and docs update.
  Files touched: adapter registry module, Antigravity adapter stub, README.md, and docs/roadmap.md.
  Test additions: opt-in stub tests and doctor visibility tests.
  Definition of done: Antigravity is discoverable only as a gated candidate and returns a clear "not yet supported" error until vendor contracts exist.

## Verification

- Keep the existing 263 offline tests green.
- Add focused tests for every sub-milestone.
- Run a real end-to-end smoke test before the v1.2.0 tag.
- Treat Simon's 2026-08-03 lesson as a release gate: mocked tests are not shipped-feature proof.
- Validate agent-os doctor against a fresh clone.
- Verify resume conflict detection against a SQLite checkpoint created before environment overrides change.
